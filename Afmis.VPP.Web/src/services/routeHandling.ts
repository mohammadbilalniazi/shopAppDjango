// import { protectedRoutePrefix } from "../routes/base";

// export const getProtectedRoute = (route: string) =>
//   "/" + protectedRoutePrefix + "/" + route;

export const getProtectedRoute = (route: string) => route;
